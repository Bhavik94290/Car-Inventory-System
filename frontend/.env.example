# Copy to frontend/.env for local overrides (git-ignored), or set this as a
# build-time environment variable in your Netlify site settings.
# Vite only reads VITE_-prefixed vars, and bakes them in at build time.

VITE_API_BASE_URL=http://localhost:8080/api
